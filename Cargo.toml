[package]
name = "simple_db"
version = "0.1.0"
edition = "2021"

[dependencies]
arrow = { version = "13", features = ["prettyprint"] }
sqlparser = "0.9.0"
log = "0.4" 
twox-hash = "1.6.3"  # 用于哈希计算
ordered-float = "3.0.0"   # 包装浮点数的方法 允许其进行排序
